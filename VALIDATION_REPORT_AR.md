# تقرير إصلاح ثيم Lumia — الإصدار 1.1.1

تم فحص الحزمة الكاملة والتأكد من وجود ملفات `src/views` ومكونات الصفحة الرئيسية والملفات المبنية داخل `public`.

## الإصلاحات المنفذة

- تعديل إصدار `@salla.sa/twilight` من إصدار غير متاح (`^2.14.525`) إلى نطاق متوافق مع الثيم الرسمي (`^2.14.501`).
- إضافة `ThemeWatcher` إلى إعداد Webpack وفق البنية القياسية لثيمات Twilight.
- تفعيل نسخ الصور تلقائياً من `src/assets/images` إلى `public/images` أثناء البناء.
- إضافة صور WebP المفقودة إلى مجلد المصدر حتى لا تختفي بعد أي بناء نظيف.
- ترتيب خطاف `body:end` قبل سكربتات الثيم لضمان تهيئة Twilight SDK أولاً.
- إضافة `body:classes` وملف الصورة البديلة الافتراضية إلى التخطيط الرئيسي.
- التحقق من أن جميع مسارات مكونات `twilight.json` تقابل ملفات Twig موجودة.
- التحقق من أن جميع ملفات الصور والـ JavaScript والـ CSS المشار إليها موجودة.

## مطلوب قبل الرفع

استبدل قيمة `repository` في `twilight.json` برابط مستودع GitHub الحقيقي، وراجع بريد ورابط الدعم. ارفع **محتويات المجلد** إلى جذر المستودع، ولا ترفع ملف ZIP كملف منفرد داخل GitHub.
